import { renderFoundation, mountFoundation } from './_renderer.js';

export const meta  = { title: 'Day 2 — Guardrails', cloud: 'home' };
export const render = () => renderFoundation(2);
export const mount  = (root) => mountFoundation(root, 2);
