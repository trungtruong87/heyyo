import { renderFoundation, mountFoundation } from './_renderer.js';

export const meta  = { title: 'Day 3 — Detection', cloud: 'home' };
export const render = () => renderFoundation(3);
export const mount  = (root) => mountFoundation(root, 3);
