import { renderFoundation, mountFoundation } from './_renderer.js';

export const meta  = { title: 'Day 4 — Landing Zone', cloud: 'home' };
export const render = () => renderFoundation(4);
export const mount  = (root) => mountFoundation(root, 4);
